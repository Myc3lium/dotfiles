#!/bin/sh

# Get user input and run.

. $HOME/.cache/wal/colors.sh
option="$(echo -e "\x00" | $HOME/.bin/dmenuw -p "» ")"
[ -z "$option" ] && exit
[ "$option" = urxvt* ] && eval $option && exit
urxvt -e bash --init-file $HOME/.bashrc -c "$option"
